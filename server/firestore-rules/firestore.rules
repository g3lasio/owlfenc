rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS PERMISIVAS TEMPORALES - Permite todo acceso autenticado
    // TODO: Implementar seguridad jerárquica más adelante
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
